<h1>Burgers!</h1>

<h2>Burgers that are hot off the grill!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been eaten.....</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>
<div class="form-box">
<h2>Let's fire up the grill!</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burge">Burger Name:</label>
    <input type="text" id="burge" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Are you ready for this order?</label><br>
    <input type="radio" name="devoured" value="0" checked="checked"> Ready, of course!!
  </div>

  <button type="submit">Add Burger</button>
</form>
</div>
